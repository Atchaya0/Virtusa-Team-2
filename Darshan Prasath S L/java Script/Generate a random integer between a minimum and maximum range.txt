function getRandomInt(min, max) {

    const minCapped = Math.ceil(min);
    const maxCapped = Math.floor(max);
    
    return Math.floor(Math.random() * (maxCapped - minCapped + 1)) + minCapped;
}

